2026-06-01 07:55 | Cloned yoropico/gureum fork (in-place), submodules + Xcode env set up.
2026-06-01 08:00 | Analysis: deployment target 10.13, Swift 5; deprecated NSUserNotification, dead Fabric/Answers, heavy Firebase.
2026-06-01 08:05 | Task: migrate NSUserNotification -> UserNotifications (UNUserNotificationCenter). SOLO mode.
2026-06-01 08:05 | Plan: refactor UpdateManager (split content/deliver), update GureumAppDelegate delegate+category, fix GureumTests; @available(macOS 10.14) gate, keep 10.13 floor.
2026-06-01 08:30 | Separate fix (build blocker): Preferences/PreferenceViewController.swift import SwiftIOKit -> import IOKit.hid. Preferences target never linked SwiftUp; relied on implicit-module transitivity that Xcode 26 explicit modules reject. Only kHIDUsage_* constants used, no SIO* symbols. Unrelated to notification migration.
2026-06-01 08:45 | Build env unblock (not repo code): fetched git tags; apple-generic versioning casts CURRENT_PROJECT_VERSION to double, so override CURRENT_PROJECT_VERSION=1.13 MARKETING_VERSION=1.13.2 on cmdline. Version.xcconfig is generated -> restored to HEAD.
2026-06-01 08:51 | build-for-testing OSX scheme SUCCEEDED with all changes (no errors/warnings in changed files).
2026-06-01 08:52 | test-without-building: testNotifyUpdate PASSED; 44/45 Swift tests pass; only pre-existing unrelated ObjC test GureumObjCTests.testIPMDServerClientWrapper fails (unsigned/sandbox-off env).
2026-06-01 08:55 | Notification migration DONE + verified. Diff = 4 files (3 notification + 1 separate SwiftIOKit build-blocker fix). Nothing committed yet.
2026-06-01 09:25 | Pushed branch to origin (yoropico/gureum) and opened fork-internal PR #1 (base main). Two commits: SwiftIOKit fix + notification migration. Auth via 1Password classic PAT (GH_TOKEN env; gh login blocked by missing read:org, bypassed since push/PR only need repo scope).
2026-06-01 10:08 | Installed debug Gureum.app to /Library/Input Methods via osascript admin (GUI auth). Ad-hoc signed + full entitlements (sandbox/IOHID/network/get-task-allow), codesign verified. Built with CURRENT_PROJECT_VERSION=1.13 override (apple-generic double-cast workaround). Pending: yoros adds input source in System Settings + verifies UN authorization prompt + debug notification.
2026-06-01 10:20 | Input source now appears after fixing dropped CFBundleVersion/ShortVersionString keys (empty ${VERSION} substitution made Xcode drop them) + lsregister -f. Reinstalled via osascript admin. Install verified working.
2026-06-01 10:25 | Session saved: refreshed .claude/session-state.md (full snapshot incl. build-env gotchas, install state, PR #1, auth). MCP sync not configured -> local only.
2026-06-01 10:48 | Cross-machine sync enabled: created .claude/devmode.json (sessionProject=gureum, env=회사PC). Pushed session to central store via MCP session_save ([gureum] 세션 생성 완료). EXAM_API_KEY still unset -> hook auto-save no-op; manual MCP push works.
2026-06-01 13:30 | Input Monitoring TCC fixed: deny was ad-hoc cdhash mismatch (multiple re-signs). User removed+re-added Gureum.app in System Settings > Input Monitoring, restarted IME (PID 11158). Verified: 0 IOHIDDeviceOpen denies after re-add (was 8). HID/CapsLock switching now works. Caveat: ad-hoc sig -> permission breaks on every rebuild; permanent fix = stable Apple Development/Developer ID signature.
2026-06-01 14:00 | DEBUGGING bct han/eng desync (UNRELATED to notif work). Root-cause investigation (systematic-debugging): enableCapslockToToggleInputMode=false + no Gureum shortcuts -> capslock switching is macOS-driven (#918 TICapsLockLanguageSwitchCapable). Symptom = composer.inputMode/delegate desync vs TIS display, fixed by re-select (setValue). Log evidence: BCT shows abnormal rapid activate/deactivate churn; some activations get NO 'Set value for tag'. Hypothesis: missed setValue during BCT focus churn leaves per-client composer in stale (qwerty) mode.
2026-06-01 14:00 | Installed DIAGNOSTIC build with temporary GRMDIAG NSLog instrumentation (UNCOMMITTED local edits in OSXCore/GureumComposer.swift, InputReceiver.swift, InputController.swift) at inputMode-setter / setValue / changeLayout / activate-deactivateServer. MUST revert before any commit/push. Analyze via: /usr/bin/log show --predicate 'eventMessage CONTAINS "GRMDIAG"'. Note: new cdhash -> Input Monitoring breaks again but capslock switching is macOS-driven so diagnostic still works.
2026-06-01 14:25 | bct han/eng FIX VERIFIED on-device (yoros: Korean now types correctly). Root cause (confirmed via GRMDIAG os_log diagnostics): (1) GureumComposer init always started at lastRomanInputMode(qwerty) -> under rapid activate/deactivate churn (Edge Beta etc.), missed setValue left composer stuck English; (2) my first fix exposed a latent ordering bug: init did 'inputMode=...; delegate=romanComposer' so the setter's correct delegate got overwritten back to roman -> 'Korean mode types English'.
2026-06-01 14:25 | FIX: add Configuration.lastInputMode (tracks last han/eng mode); GureumComposer.init sets delegate=romanComposer FIRST then inputMode=lastInputMode (setter sets correct delegate last); inputMode setter persists lastInputMode. Diagnostics (GRMDIAG/grmdiag/os.log) fully stripped. Clean diff = Configuration.swift + GureumComposer.swift only.
2026-06-01 14:36 | Shipped both fixes: PR #1 (notifications) + PR #2 (han/eng stuck, fix/hangul-english-input-stuck off main). Built+installed CLEAN combined build (both fixes, GRMDIAG stripped, UN migration present, codesign OK). Temp local branch combined-install = merge of both (NOT pushed).
2026-06-01 14:36 | Edge Korean issue = stale Chromium IME connection from repeated reinstalls (Terminal/Finder work with han3final; Edge sends no events to new Gureum). Not a code bug. Resolution: re-add input source + logout/login to reconnect all apps to final build.
2026-06-01 14:40 | RESOLVED: Korean input now works in Edge too (yoros confirmed) after final clean combined build install + reconnect. Both PRs up (#1 notifications, #2 han/eng). Bug fully fixed end-to-end. Refreshed session-state.md.
2026-06-01 14:45 | MERGED both PRs into yoropico/gureum main (HEAD 951d9f0). main now contains SwiftIOKit fix + UserNotifications migration + han/eng stuck fix. Local feature branches deleted; only main remains, synced to origin. Installed build already has both fixes. DONE.
2026-06-01 14:47 | Session saved: refreshed .claude/session-state.md to final merged state (main=951d9f0, both PRs merged, installed build has both fixes, working tree clean). Pushing snapshot to central store.
2026-06-01 (resume) | Docs: full-rewrote README.md to be fork-centric (yoropico/gureum: fork notice, changes-vs-upstream summary linking CHANGELOG, build-from-source install since no Homebrew/pkg dist) + new CHANGELOG.md (Keep a Changelog, Korean) for 1.14.0 on top of upstream 1.13.2 documenting han/eng fix (#2) and UserNotifications migration + SwiftIOKit->IOKit.hid Preferences build fix (#1). Committed 30f944d, pushed origin/main (951d9f0..30f944d). README/CHANGELOG are reader-facing -> Korean per language policy; commit msg English.
2026-06-01 (resume) | macOS cleanup/modernize: brainstorm->spec->plan->subagent-driven exec. 4 buckets on branch cleanup/modernize-macos: (C) removed dead iOS subsystem incl. iOS.xcodeproj/iOSTests/iOSTheme (code-review caught the incomplete first pass; whole subsystem gone, -10k LOC); (A) removed no-op AnswersHelper shim+callsites; (B) removed Firebase Crashlytics entirely (SPM dep, framework link, Run Crashlytics phase, GoogleService-Info.plist, FirebaseApp.configure, NSApplicationCrashOnExceptions) -> SPM graph down to MASShortcut/Fuse/Alamofire/SwiftUp; (D) MACOSX_DEPLOYMENT_TARGET 10.13->11.0 + flattened all 10.14/10.15/11.0 @available guards (kept Preferences OSX 13). Each bucket: spec+quality review subagents, signing-disabled Release build gate. Final review READY TO MERGE: 45 tests, only known testIPMDServerClientWrapper fails (unsigned, pre-existing). FF-merged to local main (b05f87d), feature branch deleted. GIT INCIDENT during Task1: a stray `git commit --amend` at HEAD=plan-commit destroyed 3b1d3bd; recovered by re-committing plan (844a77d). NOT pushed (yoros chose local merge). PENDING: on-device smoke (Korean/Eng switching, notification, Input Monitoring) needs yoros; push origin main when ready.
2026-06-01 (resume) | On-device smoke of cleanup build: built Debug from local main b05f87d, PlistBuddy-fixed CFBundleVersion/Short=1.13.2, deep ad-hoc signed w/ Gureum.entitlements+get-task-allow (codesign --verify --deep --strict VALID), installed to /Library/Input Methods via osascript admin + lsregister. Autonomous evidence: 0 Firebase symbols in installed binary, no embedded Firebase frameworks, no GoogleService plist; Gureum process launched + stable (PID 55011, no crash/fatal/firebase in logs, no DiagnosticReports). yoros confirmed on-device: Korean input + han/eng switching normal so far. => cleanup/modernize VERIFIED end-to-end (artifact + runtime + human input). PENDING: push local main (6 ahead of origin/main 30f944d) on yoros's go.
2026-06-01 (resume) | Finalized cleanup/modernize: (1) pushed local main to origin (30f944d..b05f87d, origin/main now == b05f87d). (2) Built RELEASE from b05f87d (signing-disabled), PlistBuddy-fixed CFBundleVersion=1.13.2 (git-describe gave non-numeric "1.13.2-28-gb05f87d-Debug"), deep ad-hoc signed w/ OSX/Gureum.entitlements (no get-task-allow), codesign --verify --deep --strict VALID. Verified Release artifact: 0 Firebase symbols, 0 '디버그 빌드 알림' string (Release has no debug launch notice / key-logging warning), only libswift_Concurrency.dylib embedded (Swift runtime in-OS at 11.0 floor). Installed to /Library/Input Methods via osascript admin + lsregister; installed copy VALID, v1.13.2, 0 Firebase. Process launches on focus (Release not auto-running post-killall). REINSTALL changed cdhash again -> yoros must re-grant Input Monitoring (+ maybe re-add input source / logout-login) for CapsLock han/eng. cleanup/modernize fully DONE: merged, pushed, Release installed.
2026-06-01 (resume) | yoros confirmed Release build (b05f87d) displays/works normally on device after reinstall + permission re-grant. cleanup/modernize COMPLETE end-to-end (merged, pushed origin/main==b05f87d, Release installed + verified). Session work done.
2026-06-01 (resume) | Docs update for cleanup: added CHANGELOG [1.15.0] (Firebase Crashlytics removal, dead AnswersHelper, orphaned iOS subsystem ~10k LOC, macOS 10.13->11.0 + guard flatten) + README updates (macOS 11.0+ badge/requirement, fork-notice wording, lighter/no-Firebase note, version-grouped changes 1.15.0/1.14.0). Committed 66c79d1, pushed origin/main (b05f87d..66c79d1). NOTE: CHANGELOG compare links use 1.14.0...main / 1.13.2...1.14.0 but no git tags 1.14.0/1.15.0 exist yet -> offered to cut tags.
2026-06-01 (resume) | Cut + pushed annotated tags 1.14.0 (30f944d) and 1.15.0 (66c79d1=HEAD) so CHANGELOG compare links (1.13.2...1.14.0, 1.14.0...main) resolve on GitHub. cleanup/modernize fully shipped: code merged+pushed, Release installed+verified on device, README/CHANGELOG documented, releases tagged.
2026-06-01 (resume) | Stable Apple Development signing set up (personal use, free). Diagnosis: cert "Apple Development: yoropico@gmail.com (K83K59TGLX)", team G7J2LY4LP9, existed w/ private key but find-identity=0 valid -> CSSMERR_TP_NOT_TRUSTED because the issuing WWDR G3 intermediate was missing (only the expired 2023 G1 was present). Fix: downloaded Apple's official AppleWWDRCAG3.cer (https://www.apple.com/certificateauthority/AppleWWDRCAG3.cer, valid 2020-2030), `security import` into login.keychain -> 1 valid identity. Built Release with Xcode automatic signing (DEVELOPMENT_TEAM=G7J2LY4LP9 CODE_SIGN_STYLE=Automatic CODE_SIGN_IDENTITY="Apple Development" -allowProvisioningUpdates) into build/DerivedData-signed; codesign needed login-keychain password + "Always Allow" (yoros granted). Result: properly Apple-Development-signed (chain to Apple Root CA), CFBundleVersion=1.15.0 (git-describe clean thanks to the 1.15.0 tag), entitlements intact, 0 Firebase, no embedded.provisionprofile but launches with no amfi/profile/sandbox-deny errors. Installed to /Library/Input Methods. Going forward this stable identity should make Input Monitoring TCC persist across rebuilds (no more churn). yoros must re-grant Input Monitoring ONCE for the new identity. Cert expires 2027-05-31.
2026-06-02 | Auto-updater feature: implemented all 6 AU tasks via subagent-driven dev on feature/github-auto-updater (off main 66c79d1). Commits: 4c13af3 spec, 2c02eab plan, acb5a77 isNewer, ac9cd73 GitHub Releases API, 7b079f2 auto-check+notif userInfo, a2b3e29 Updater, 6cf6b5b harden Updater (pipe-deadlock + shell-injection + temp cleanup, from code review), cb6554e wire menu/notif, b8926e4 non-sandbox entitlements. AU-1/2/3/4 done via subagents w/ spec+quality reviews (AU-4 had a review fix). AU-5 applied INLINE by controller (subagents hit repeated API socket/529-overload errors); verified by build + grep self-review. AU-6: created OSX/Gureum-personal.entitlements (empty=non-sandbox), built signed non-sandbox Release (Apple Development G7J2LY4LP9, app-sandbox absent, Updater symbols present, universal) into build/DerivedData-updater. GOTCHA: CODE_SIGN_ENTITLEMENTS override must be ABSOLUTE path (relative breaks Hangul/Alamofire/MASShortcut subprojects). Tests: testIsNewer/testGitHubReleaseDecodeAndAsset/testNotifyUpdate pass; only known unsigned GureumObjCTests.testIPMDServerClientWrapper fails. Branch PUSHED to origin. PENDING (yoros): (1) open PR — gh not authed + 1Password PAT extraction blocked by auto-mode classifier; web compare link given; (2) on-device e2e (install non-sandbox build + publish a test release tag>1.15.0 + verify 1-click update). Final whole-branch review was self/grep (subagent review blocked by 529 overload).
2026-06-02 | Auto-updater e2e PASSED on device (yoros confirmed). Bug found during e2e: "업데이트 확인" returned "정보에 접근할 수 없습니다" because Alamofire .validate() checks response Content-Type against the request Accept header (application/vnd.github+json) but GitHub returns application/json -> content-type validation failed -> nil. Fix b80d0e5: .validate(statusCode: 200..<300). Rebuilt fixed code, reinstalled 1.15.0 (non-sandbox Apple Development signed), refreshed the 1.16.0 GitHub release asset (byte-match). Re-test: detect 1.16.0 -> 지금 업데이트 -> install -> works. Branch feature/github-auto-updater pushed to b80d0e5 (10 commits over main). No PR open yet. PENDING finish: open/merge PR; delete test release 1.16.0 + tag + local Gureum-1.16.0.zip.
2026-06-02 | rightToggleKey (right Cmd = 한/영) DEBUG+RESOLVED. Symptom: after install, neither right-Cmd nor CapsLock toggled; each app stuck one mode. Root cause: Input Monitoring (kTCCServiceListenEvent) denied -> IOHIDManager opens ONCE at InputMethodServer init, so a process started while denied stays denied forever (logs: "TCC deny IOHIDDeviceOpen" only at startup, no retry). Not code: rightToggleKey HID path + InputReceiver selectMode are upstream/correct. Fix: grant Input Monitoring THEN restart the IME (killall Gureum -> relaunch on focus) so the new process opens HID with permission. Confirmed: right-Cmd english<->한글 switching works. NOTE gotcha: Input Monitoring must be active BEFORE the IME process starts; granting alone isn't enough, must restart IME. Stable Apple Development sig means it persists after this one clean grant+restart. The right-toggle-key popup (Preferences): index1=RightGUI(Cmd)/2=RightAlt(default)/3=RightControl/0=none; MASShortcut "입력기 바꾸기 단축키" is the keyDown path (no Input Monitoring needed) e.g. Shift+Space.
2026-06-02 | ime-state-icon RESOLVED (config, not code). Right-Cmd toggled the input but the menu-bar icon didn't update because the user had only 영문(ABC)+구름(한글) enabled; Gureum's rightToggleKey does an INTERNAL composer han<->roman toggle + selectMode(lastRomanInputMode=...qwerty), but the Gureum roman source wasn't enabled so selectMode couldn't move the system icon. Fix: user added the "구름 로마자(QWERTY)" input source -> internal toggle now moves between two ENABLED 구름 sources -> icon updates. Confirmed working. Note: right-Cmd toggle only fires while a 구름 source is active (Gureum receives events); ABC active = no toggle, so use 구름 로마자 for English. This is upstream Gureum + macOS input-source config, unrelated to cleanup/auto-updater code.
2026-06-02 | Auto-updater SHIPPED. Added update-complete alert guidance (input-source re-add + Input Monitoring re-grant + IME restart) commit d9c2a93. Opened PR #3, merged (merge commit 0a718d5) into origin/main, deleted feature branch (local+remote). Cleaned up e2e test release: gh release delete 1.16.0 --cleanup-tag (releases/latest now 404, remote 1.16.0 tag gone), removed local Gureum-1.16.0.zip. origin/main == local main == 0a718d5. Feature complete: GitHub Releases auto-update (check + 1-click download/verify/install) implemented, hardened, e2e-verified on device, documented, merged. PR/release ops used the git-credential GitHub token (gh unauthenticated); transient auto-mode classifier denials cleared on retry.
2026-06-03 | menu-bar IME icons SIMPLIFIED to monochrome template (branch feature/menubar-icon-simplify). Brainstorm->spec->plan (docs/superpowers/{specs,plans}/2026-06-03-*) then inline-executed. Generator OSX/Icons/generate-menubar-icons.swift renders the 8 indicator imagesets (eng/qwerty/han/han2/han3/han390/han3final/hanroman) to black+alpha PNGs (@1x16/@2x32); Contents.json gets template-rendering-intent:template (system tints black on light bar / white on dark). NO Info.plist or code change (filenames unchanged; icons live only in Assets.car, resolved by asset-catalog name). FINAL design after heavy on-device iteration: Korean(ko)=FILLED cloud (SF Symbol cloud.fill), English(en)=OUTLINE cloud (SF Symbol "cloud"), NO inner label (mode name already shows beside icon in the input menu/picker; menu bar only needs Han-vs-Eng). Classified by Info.plist TISIntendedLanguage. Sizing = tight-bbox aspect-fit, margin 0 (no clip): clouds are wider than tall so this fills width edge-to-edge (~70% height); enlarging further clips/distorts (rejected on device). Evolution that DIDN'T work (do not retry): (1) cloud+single-char knockout label (2/3/9/F/R/Q/안) — busy at 16px, 안 illegible; (2) hand-drawn bezier cloud — read as a "house"; (3) height-filling cloud.fill — side clip turned it into a block. Verify trick: /tmp/gureum-icon-verify/verify.swift loads the committed PNGs and simulates template tint on light+dark for visual review (Read the contact.png). Built signed Release + installed (gotcha #11 cmd, gotcha #1 osascript admin). PENDING: on-device dark-mode confirm (R1) + finalize branch (merge/PR). Generator uses SF Symbols only at generation time -> output is static PNG, no runtime dependency / deployment-target concern.
2026-06-03 | menu-bar cloud REWORK shipped (origin/main == 40e7b49). User reported the installed cloud rendered SQUISHED. Root cause (systematic-debugging): the TIS input-source menu-icon slot (Info.plist tsInputModeMenuIconFileKey) is a FIXED SQUARE — a brief wide 24x16 generator experiment got aspect-distorted into it (confirmed via assetutil: installed Assets.car held 24x16; committed cfcb945 was 16x16; the 24x16 PNG itself was undistorted -> distortion is at display). Fix in TWO layers: (L1) system 8 imagesets stay SQUARE, now render Apple SF Symbol icloud/icloud.fill (ko=fill, en=outline) tight-bbox aspect-fit + letterboxed -> undistorted but a bit short. (L2) NEW NSStatusItem in OSX/GureumAppDelegate.swift (CloudStatusItemController) draws the icloud SF Symbol DIRECTLY (status slot is width-flexible -> wide cloud undistorted, like OneDrive which is a status-bar app not an input-source icon). icloud.fill=Korean/icloud=English, pointSize 21 (15 was too small vs wifi/battery — cloud is wide+short so needs a larger pointSize to match height). Updated via distributed notif kTISNotifySelectedKeyboardInputSourceChanged (no input hot-path edit: Gureum already selectMode()s, which fires it); hidden when current source is non-Gureum; click -> small menu (환경설정/정보). Pure classify(inputSourceID:primaryLanguage:) verified standalone 9/9. NO Info.plist/hot-path/pbxproj change (code in existing GureumAppDelegate.swift, assets in existing .xcassets). Dead ends rejected: hand-built single-bezier (Catmull-Rom) cloud and circle-union+morphology-outline cloud both looked less pretty than the Apple icloud symbol; a wide statushan/statuseng PNG pair was added then removed (status item uses the symbol directly). Specs: docs/superpowers/specs/2026-06-03-menubar-cloud-square-redesign.md + 2026-06-03-menubar-statusitem-onedrive-cloud-design.md. Commits 8e25929 (feature) + 40e7b49 (21pt). PENDING (user's eyes): on-device confirm status item appears from the agent process (R1) + 21pt size. NB: push to main needs FRESH per-instance user authorization (auto-mode classifier denied the 2nd push until re-asked).
2026-06-04 | RESOLVED bomi-input "can't type Korean" (long-running blocker across many sessions). systematic-debugging: gathered evidence FIRST instead of guessing. Smoking gun in `log show --predicate 'process == "bomi-input"'`: `Fault (AppKit) [Nib Loading] Unknown class '_TtC6Gureum17GureumAppDelegate', using 'NSObject' instead. ... MainMenu.nib`. ROOT CAUSE: rebrand set PRODUCT_NAME=bomi-input -> app target Swift module Gureum->bomi_input, but OSX/MainMenu.xib hardcoded the app delegate customModule="Gureum" with NO customModuleProvider="target". Class unresolved -> AppKit substitutes NSObject -> GureumAppDelegate never instantiated -> applicationDidFinishLaunching never runs -> `_ = InputMethodServer.shared` (IMKServer creation) never runs -> IME process launches (NSApp from nib) but the input pipeline is dead, selected source reverts to com.apple.keylayout.ABC, NO custom logs (BDIAG was never CALLED, not "suppressed" as the prior session wrongly hypothesized). Why only the delegate broke: OSX/Configuration.storyboard + Preferences.xib already carried customModuleProvider="target" (auto-tracks the build module); MainMenu.xib was the lone xib missing it. FIX (1 line): MainMenu.xib -> customModule="bomi_input" customModuleProvider="target". Verified at THREE levels: (a) built nib `strings` contains `_TtC10bomi_input17GureumAppDelegate` (correct module); (b) post-install `log show` shows applicationDidFinishLaunching START + InputMethodServer BEFORE/AFTER IMKServer created + io=ok, and the "Unknown class" fault is GONE; (c) user confirmed Korean typing works + handle->composer logs show keystrokes reaching the composer (inputMode han3final, delegate Optional<Composer>). Then REMOVED all BDIAG instrumentation (it logged keyCodes per keystroke = privacy) and rebuilt clean (BDIAG strings: 0). Committed e67b01d on main (also bundles: suite isolation com.yoropico.bomi-input; io optional no-force-unwrap; full-color status icons ㅂ/B + dark variants; new CLAUDE.md w/ build+install flow and the xib module gotcha). Build/sign/install per CLAUDE.md (project renamed to bomi-input.xcodeproj, scheme OSX; restored Version.xcconfig out of the commit). NOT pushed (push needs fresh user auth). Open: optional clean-rename of internal InputMethodConnectionName (kept GureumInputMethod_1_Connection = verified value, invisible) + GureumObjCTests.m still imports Gureum-Swift.h (test target only).
2026-06-04 (cont) | BCT(claude-terminal) 한글 Enter 지연 — 정확한 원인 규명 후 정공법 해결. ROOT CAUSE: macOS Ime::Commit 에 종료키가 안 실려 BCT 는 NSEvent 모니터의 IME_RETURN_DETECTED(keyCode 36) 로 Return 종료를 판별 + 20ms 타이머(IME_COMMIT_ENTER_DELAY)로 \r 전송했는데, 이 플래그가 IME commit 일 때만 소비돼 raw 키로 처리된 Return(예: 셸 명령 실행 Enter)이 stale 로 남음 → 다음 한글 음절-분리 commit 이 stale ret 을 보고 팬텀 \r 발사. 기존 "새 preedit 이 타이머 취소" 가드는 이를 마스킹만(+빠른 연타 시 Enter 유실 위험). systematic-debugging: cat>file 바이트 probe(이중 newline 검출) + BCT_IME_TRACE env-gated 로그(handle_ime_commit/preedit/keyboard_input/timer)로 경로 확정 — 옵션 A(즉시전송) 1차 시도가 probe 로 팬텀(안\n녕\n) 잡힘 → stale 플래그가 진짜 원인 확인. FIX(claude-terminal master 36e700f, 로컬): (1) handle_keyboard_input 진입 시 raw keyDown 마다 IME_RETURN/ESC_DETECTED 리셋 → stale 원천 차단(조합 종료 Enter 는 Ime::Commit 으로 와서 여기서 안 지워짐). (2) handle_ime_commit 에서 ret 이면 commit 직후 즉시 \r(타이머 제거) → 지연 20ms→0ms. (3) 죽은 터미널 ime_commit_time 타이머·WaitUntil 분기·Pane 필드 제거(launcher 타이머 유지). 검증: probe3 = 안녕\n(단일), trace 안=send_enter=false(차단)/녕=true(즉시). cargo build clean. 진단 로그 전량 제거. bomi 는 미수정(이미 processed:false 로 Return 통과하나 winit 가 KeyboardInput 으로 안 줌 — winit 계층 한계라 BCT-side 해결). 다른 세션이 release 빌드/설치로 일상 반영. // gureum: de6ef3e(새 ㅂ/B 메뉴바 아이콘 from assets 2) + 80a921d(docs/research DKST 분석) push 완료, origin/main==80a921d. DKST 인라인 입력 기능은 brainstorming 보류(조사: docs/research/2026-06-04-dkst-source-analysis.md). bomi SearchComposer 가 이미 한자/이모지/MS특수문자(hanjar/c/w.txt, emoji*.txt, mssymbolr.txt) 보유 — DKST 신규는 인라인·Shift+자모·사용자사전뿐.
2026-06-04 (cont) | INLINE DIRECT-INPUT P1 — shipped via devmode team mode (planner→implementer×N→tester→reviewer). Commit ecd254f (local; spec c3230ca). DKST-style (MIT) no-underline composition behind default-FALSE kill-switch inlineCompositionEnabled. NEW OSXCore/InlineComposition.swift: pure classifyComposition() P1 chain (alwaysMarked → showsComposingTextAsMarkedText private API → selectedRange-queryable → default inline; P2 WebKit/Chromium/blocklist stubbed). InputController: directRange/directText + directRangeIsCurrent validation + LiveClientCapabilities (ALL dynamic IMK probes responds(to:)-guarded — a prior unguarded value(forKey:"textDocument") crashed all input, fixed) + useMarkedText cached on activate/mode-change, reset on deactivate/cancel/mode-change. InputReceiver: renderInline() single insertText(commit+composed, replacementRange) + shared inlineReplaceRange() (validate directRange else selectedRange backtrack); commitCompositionEvent made INLINE-AWARE (reviewer must-fix: forced commits — han/eng toggle/deactivate/mouseDown/candidate/external — were double-inserting the inline syllable "안"→"안안"; now replaces directRange in place). Verified: inline-render recorded sequence ("ㅇ",NF)("아",{0,1})("안",{0,1})("안ㄴ",{0,1})("녀",{1,1})("녕",{1,1}) → "안녕"; no-duplicate-on-forced-commit tests pass. Tests via extended MockInputClient (records insertText+setMarkedText, honors replacementRange). Also fixed OSXTests rebrand misses: BUNDLE_LOADER → bomi-input.app/bomi-input (pbxproj), GureumObjCTests import → bomi_input-Swift.h. OSXTests: 55 pass / 1 known baseline (testIPMDServerClientWrapper unsigned). Reviewer APPROVE. NOT pushed (origin 80a921d; local +c3230ca +ecd254f). CAVEAT: kill-switch defaults FALSE = dormant in prod; P2 (WebKit/Chromium engine policy) not implemented, so enabling on-device now would default Chromium/Electron apps to inline (DKST marks them) — do P2 before broad dogfood. Spec: docs/superpowers/specs/2026-06-04-inline-direct-input-design.md (P1/P2/P3 phasing). DKST research: docs/research/2026-06-04-dkst-source-analysis.md.
2026-06-05 | UI cleanup (UNCOMMITTED, built+installed): (1) Removed CloudStatusItemController — the separate NSStatusItem ㅂ/B menu-bar indicator (user: "메뉴바에 아이콘이 따로 표시 되는 기능을 제거"). OSX/GureumAppDelegate.swift: deleted the class + currentInputSourceInfo() helper + statusController property + its instantiation in applicationDidFinishLaunching, and dropped `import Carbon` (only the status item's TIS APIs used it). Build OK — IOHIDRequestAccess does NOT need Carbon (it predated the status-item work). Standard input-source menu icon (han/eng/han2/… imagesets) is a SEPARATE mechanism and stays. Orphaned: statusbomi_han/statusbomi_eng imagesets now unused (kept for now, pending user decision). (2) Rebranded remaining user-visible "구름"→bomi-input (user: "옵션창도 ... 아직 구름이라고 표시"): OSX/Configuration.storyboard window title "구름 환경설정"→"bomi-input 환경설정"; Preferences/Base.lproj/Preferences.xib two settings-help strings; OSX/MainMenu.xib "구름 입력기에 관하여"→"bomi-input에 관하여"; OSX/GureumMenu.swift update-check alert texts (2 spots). Verified no user-visible 구름 remains in xib/storyboard/strings/swift-literals; README "구름" intentionally kept (correctly describes the fork); 3 code comments (GureumComposer.swift, KeyCode.swift) left (non-visible). Built signed Release + installed (/Library/Input Methods/bomi-input.app, pid live). 5 changed files uncommitted: GureumAppDelegate.swift, Configuration.storyboard, Preferences.xib, MainMenu.xib, GureumMenu.swift. origin/main==ecd254f; this cleanup not yet committed/pushed.
2026-06-05 (cont) | UI cleanup COMMITTED + PUSHED (origin/main a14dc5d). bfe31e9: status-item removal + removed 4 input modes (deprecated roman qwerty/dvorak/colemak + test han3layout2) from Info.plist ComponentInputModeDict + tsVisibleInputModeOrderedArray + ko/en InfoPlist.strings (sed line-delete from original line numbers: Info.plist 52,97/288,312/382,406/411,413; strings 4,6/13; plutil OK; 16→12 modes; English now `system`="로마자"; Swift enum cases KEPT since RomanComposer uses .qwerty.keyboardData as reference) + all user-visible 구름→bomi-input (Configuration.storyboard/Preferences.xib/MainMenu.xib/GureumMenu.swift). a14dc5d: trimmed input-menu web links — removed 웹사이트(gureum.io)/도움말(dan.gureum.io)/후원하기(donation.gureum.io) items + openWebsite/Help/Donation methods; kept 소스 코드→github.com/yoropico/bomi-input + 버그 알리기→.../issues (retargeted from ssi/meok.gureum.io). Each step: built signed Release + installed (gotcha #1 osascript admin, restored Version.xcconfig). DIAGNOSIS: DKST feature port = inline P1 only (ecd254f, DORMANT behind default-false inlineCompositionEnabled); NEXT = inline P2 (WebKit/Chromium engine policy + blocklist) before on-device enable, then P3 (leak/eager-sync/settings UI); Shift+jamo + user-dictionary not started. bomi already has hanja/emoji/symbol search (SearchComposer).
2026-06-05 (cont) | Inline direct-input P2 (engine policy + config) implemented via TDD (UNCOMMITTED). Re-cloned DKST (MIT) to /tmp/dkst-research for exact lists. RED→GREEN→Release-build. (1) OSXCore/InlineComposition.swift: filled the P1-reserved P2 hook. New PURE classifiers ported from DKST InputController.m — bundleIdentifierUsesWebKitTextStack (com.apple.Safari/WebKit/mobilesafari, exact-or-prefix+"." dot-boundary), bundleIdentifierUsesChromiumMarkedTextPolicy (org.chromium.Chromium/com.google.Chrome(.canary)/edgemac/brave/vivaldi/opera/naver.Whale/Arc=company.thebrowser.Browser/perplexity.comet/openai.atlas variants), bundleIdentifierMatchesForcedMarkedList (case-insensitive membership). classifyComposition chain now: 1 alwaysMarkedGlobal→marked, 2 showsComposingTextAsMarkedText non-nil→reflect, 3 forced-list→marked (user override, before WebKit per DKST _forcedMarkedTextBundleIDs@882), 4 WebKit→inline, 5 Chromium prefix OR framework-scan→marked, 6 !selectedRangeQueryable→marked, 7 default→inline. ClientCapabilities protocol +forcedMarkedBundleIDs/+usesChromiumFrameworkTextStack with extension DEFAULTS ([] / false) so P1 stubs + any conformer still compile. (2) OSXCore/InputController.swift LiveClientCapabilities: forcedMarkedBundleIDs←Configuration; usesChromiumFrameworkTextStack = port of DKST runningApplicationUsesChromiumTextStack(515)+applicationBundleUsesChromiumTextStack(455) — NSRunningApplication.runningApplications(withBundleIdentifier:), comet/atlas name check, then .app/Contents/Frameworks scan vs known Electron/Chromium framework dirs (+Chromium/Electron substring), static chromiumDetectionCache keyed by bundleID (main-thread, no lock). (3) OSXCore/Configuration.swift: key inlineCompositionForcedMarkedBundleIDs ("InlineCompositionForcedMarkedBundleIDs"), default [], stringArray getter/setter. (4) GureumTests/InlineCompositionTests.swift: StubCaps gained forcedMarked/chromiumFramework; +9 tests (3 pure-helper + 6 classify-branch incl. forced-beats-WebKit, WebKit-beats-selectedRange-fallback, Apple-signal-beats-Chromium ordering, framework-scan→marked). RED verified (18 assert failures, 0 unexpected) then GREEN. OSXTests Debug: 65 run / 1 failure = only testIPMDServerClientWrapper (known unsigned baseline), 0 unexpected. Release BUILD SUCCEEDED (only pre-existing resolveTextDocument controller-as?-NSObject warning). Kill-switch still default FALSE = dormant in prod. Spec status updated (P1+P2 DONE, P3 pending). NOT committed (origin a14dc5d; local +4 files). NEXT: commit P2, then P3 (leak/eager-sync/Preferences UI) before flipping switch ON.
2026-06-05 (cont) | Inline direct-input P3 — UI done, hot-path hardening DEFERRED by user decision. Investigated DKST P3 funcs (repairFirstMarkedTextLeakForClient:954 = MARKED-mode Chromium first-roman-leak fix needing DKSTRomanStringForHangulKeyCode port; syncInputClient:force:1010 + shouldAvoidEagerSyncForClient:420 = US-keyboard override throttle/avoid-for-WebKit; resetCompositionState:1033 = engine+directInput reset) and found they're tied to DKST's own engine + SPECULATIVE for bomi (libhangul+IMKit); P1 already covers the reset cases its tests prove. User chose "Preferences UI only" (defer speculative hot-path edits to the rebrand-bug area until on-device dogfooding shows real problems). Implemented (5102d16, LOCAL — feature+spec): Preferences/PreferenceViewController.swift gets a programmatically-built "인라인 직접 입력 (실험적)" section appended to the settings stack view via ONE new xib outlet (inlineSettingsStackView→kLe-bm-FOO; NO fragile control XML added to Preferences.xib — chosen because Preferences pane is scrollable stackView so a code-built section fits, and CLAUDE.md warns xib editing once killed all input). Controls: master enable (inlineCompositionEnabled), global always-marked (inlineCompositionAlwaysMarked) NSButton checkboxes, + newline-separated bundle-ID editor (NSTextView via NSTextView.scrollableTextView, NSTextViewDelegate.textDidChange saves normalized list; does NOT reformat live to avoid cursor disruption). TDD: +4 tests for parse/format normalization (split/trim/drop-blank, case-insensitive dedupe keeping first, empty→[], join). KEY build-graph gotcha: parseForcedMarkedBundleIDList/formatForcedMarkedBundleIDList had to live in Configuration.swift NOT InlineComposition.swift — the legacy prefpane target (Preferences, module GureumPreferences, OTHER_SWIFT_FLAGS -DUSE_PREFPANE) does NOT import GureumCore and compiles only a SUBSET of OSXCore in-module (Configuration.swift yes, InlineComposition.swift no); the OSX scheme builds that prefpane as a dependency so it must compile. First attempt (helpers in InlineComposition.swift) → "cannot find ...in scope" in the prefpane compile; moved to Configuration.swift → all targets resolve. Verified: OSXTests 69 run / 1 failure (known unsigned testIPMDServerClientWrapper baseline), testPreferencePane PASS (xib outlet loads + viewDidLoad builds section, no crash), Release BUILD SUCCEEDED, signed Release installed to /Library/Input Methods (pid live, no Unknown class/nib errors in log). Visual layout NOT screenshot-verified (IME settings window opens only via input-menu→환경설정; headless automation unreliable) — functional wiring verified; user to eyeball + iterate. NEXT: push 5102d16 (local), then enable inline via the new UI + on-device dogfood matrix; port hot-path hardening only if real problems surface.
